import type { NextPage } from "next";
import Head from "next/head";
import { type } from "os";
import text from '../env/testInput.js'; // Relative path to your File
import React,{ useState } from "react";
console.log(text); 


const file: string = text;
const octopiMatrix: number[][] = file.split("\n").map((x) => x.trim().split("").map(y=>y.trim()).map(z=>parseInt(z)));
console.log(octopiMatrix);
const increaseEnergy = (matrix:number[][]) =>  matrix.map(row=>row.map(num=>num+1))

const Home: NextPage = () => {
  const [octoMatrix,setOctoMatrix] = useState(octopiMatrix)
  // init()
  const handleIterate = () => {
    setOctoMatrix(increaseEnergy(octoMatrix));
  };
  return (
    <>
    
      <Head>
        <title>Dumbo Octopus</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className="container mx-auto flex flex-col items-center justify-center min-h-screen p-4">
        <h1 className="text-5xl md:text-[5rem] leading-normal font-extrabold text-gray-700">
          <span className="text-purple-300">Dumbo</span> Octopus
        </h1>
        <p className="text-2xl text-gray-700">This stack uses:</p>
        <Button onClick={handleIterate} text={"iterate"} />
        <div><OctupusGrid matrix={octoMatrix} /></div>
        <div className="grid gap-3 pt-3 mt-3 text-center md:grid-cols-3 lg:w-2/3">
          <TechnologyCard
            name="NextJS"
            description="The React framework for production"
            documentation="https://nextjs.org/"
          />
          <TechnologyCard
            name="TypeScript"
            description="Strongly typed programming language that builds on JavaScript, giving you better tooling at any scale"
            documentation="https://www.typescriptlang.org/"
          />
          <TechnologyCard
            name="TailwindCSS"
            description="Rapidly build modern websites without ever leaving your HTML"
            documentation="https://tailwindcss.com/"
          />
          <TechnologyCard
            name="tRPC"
            description="End-to-end typesafe APIs made easy"
            documentation="https://trpc.io/"
          />
          <TechnologyCard
            name="Next-Auth"
            description="Authentication for Next.js"
            documentation="https://next-auth.js.org/"
          />
          <TechnologyCard
            name="Prisma"
            description="Build data-driven JavaScript & TypeScript apps in less time"
            documentation="https://www.prisma.io/docs/"
          />
        </div>
      </main>
    </>
  );
};

type ButtonProps = {
  onClick: any;
  text: string;
};

const Button = ({ onClick, text }:ButtonProps) => <button onClick={onClick}>{text}</button>;

export default Home;

type TechnologyCardProps = {
  name: string;
  description: string;
  documentation: string;
};

const TechnologyCard = ({
  name,
  description,
  documentation,
}: TechnologyCardProps) => {
  return (
    <section className="flex flex-col justify-center p-6 duration-500 border-2 border-gray-500 rounded shadow-xl motion-safe:hover:scale-105">
      <h2 className="text-lg text-gray-700">{name}</h2>
      <p className="text-sm text-gray-600">{description}</p>
      <a
        className="mt-3 text-sm underline text-violet-500 decoration-dotted underline-offset-2"
        href={documentation}
        target="_blank"
        rel="noreferrer"
      >
        Documentation
      </a>
    </section>
  );
};

type OctopusProps = {
  energyLevel: number;
};

const Octopus = ({energyLevel}:OctopusProps) =>{
  return (
  <div className="grid gap-3 pt-3 mt-3 text-center md:grid-cols-3 lg:w-2/3">
    {energyLevel}  
  </div>  )

}

type OctopusGridProps = {
  matrix: number[][];
};
const OctupusGrid = ( {matrix}:OctopusGridProps)=>{
  
    return(
    <div className="flex flex-row">
      {matrix.map((numArray,yindex)=>{
        return(
          <ol key={yindex.toString()}>
            {numArray.map((num,xindex)=>{return <li className="border-gray-500 w-10 h-10 " key={xindex+","+yindex}>{num}</li>})}
          </ol>
        )
      })}
    </div>
    )
   

}